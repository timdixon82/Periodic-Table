# Dependabot configuration.
# Weekly grouped updates for GitHub Actions and npm.
# The composer ecosystem is excluded because this project uses no PHP.
version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    groups:
      github-actions:
        patterns: ["*"]

  - package-ecosystem: npm
    directory: /
    schedule:
      interval: weekly
    groups:
      javascript:
        patterns: ["*"]
